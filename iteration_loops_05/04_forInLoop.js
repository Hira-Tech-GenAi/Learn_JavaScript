// for in loop

const myObject = {
  js: "javascript",
  ts: "typescript",
  cpp: "c++",
  rb: "ruby",
};

for (const key in myObject) {
//   console.log(`${key} is for ${myObject[key]}`);
}

const programming = ["hira", "ali", "sehar", "bilo"]
for (const key in programming) {
    console.log(programming[key]);

    
}